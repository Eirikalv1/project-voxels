[package]
name = "threed-bevy"
version = "0.1.0"
edition = "2021"

[profile.dev]
opt-level = 1

[profile.dev.package."*"]
opt-level = 3

[dependencies]
bevy = {version = "0.9.1", features = ["dynamic"]}
bevy-inspector-egui = "0.17.0"
bevy_flycam = "0.9.0"
noise = "0.8.2"
